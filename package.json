{
  "name": "lol-loot-simulator",
  "version": "1.0.0",
  "description": "League of Legends Loot Simulator",
  "scripts": {
    "docker:up": "docker compose up -d",
    "docker:logs": "docker compose logs -f",
    "docker:down": "docker compose down --volumes",
    "docker:rebuild": "npm run docker:down; npm run docker:up",
    "docker:restart": "docker compose restart",
    "test:all": "cd service-account; npm run test; cd ../service-catalog; npm run test; cd ../service-inventory; npm run test; cd ../service-loot; npm run test; cd ../service-store; npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@commitlint/cli": "^18.1.0",
    "@commitlint/config-conventional": "^18.1.0",
    "husky": "^8.0.3"
  }
}
